@{
    ViewBag.Title = "PaymentConfirm";
    Layout = "~/Views/Shared/_LayoutUser.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h3 class="text-center">Kết quả thanh toán</h3>
                </div>s
                <div class="card-body">
                    <div class="alert @(ViewBag.Message.Contains("thành công") ? "alert-success" : "alert-danger")">
                        @ViewBag.Message
                    </div>
                    <div class="text-center mt-4">
                        <a href="@Url.Action("Index", "ShopGiay")" class="btn btn-primary">Quay về trang chủ</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>